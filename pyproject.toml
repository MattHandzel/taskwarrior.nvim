[tool.ruff]
line-length = 100
target-version = "py38"
exclude = [".venv", "__pycache__", ".pytest_cache"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "UP"]
ignore = ["E501"]  # line length is handled by formatter

[tool.ruff.format]
quote-style = "double"
